# API URL for the frontend.
# For local development, use /api/v1 (proxied via vite.config.js)
# For production, set this to the unified domain API URL, e.g., https://glikourl.site/api
VITE_API_URL=https://glikourl.site/api
